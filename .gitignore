# Data — never committed. Rebuild with ml/download_dataset.sh
data/
work/
*.zip
*.tar
*.tar.gz

# Model weights and training output
*.pdparams
*.pdmodel
*.pdiparams
output/
pretrain/
inference/

# Reference PDFs (rulebook is public; idea draft lives in Drive)
docs/*.pdf

# Secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store

# Original notebook — still contains the un-rotated API key.
# Tracked copy lives at notebooks/01_eda.ipynb (scrubbed).
clyde's.ipynb

# Model weights — 114 MB exceeds GitHub's 100 MB file limit
serving/models/rec/
